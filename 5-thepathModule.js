const path = require("path");

const pathName = path.join("/Fundamentals-P1/", "starter", "text.txt");
console.log(pathName); // joined the path with the default sys seperator.

console.log(path.basename(pathName)); // the base of the path.

// resolve and something else.
